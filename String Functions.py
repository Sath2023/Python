str = "democracy"
print(str)
print(str.endswith("cy")) # Checks whether the string ends with the specified substring
print(str.capitalize()) # Capitalizes only the first character
print(str.find("a")) # Returns the index of the specified character or string where it has occured first
# In case if the character or substring doesnot excists it returns -1
print(str.replace("a","e")) # It can replace either a single character or a substring
print(str.count("c")) # Counts the presence of the specified substring or character
